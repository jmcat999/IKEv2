charon {
    load_modular = yes

    # Some mobile networks and Android/Windows clients drop larger IKE_AUTH fragments.
    # Keep IKEv2 fragments small enough for UDP/4500 over NAT.
    fragment_size = 576

    plugins {
        include strongswan.d/charon/*.conf
    }
}

include strongswan.d/*.conf
